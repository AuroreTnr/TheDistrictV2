{% extends 'base.html.twig' %}

{% block title %}S' inscrire{% endblock %}

{% block body %}
<div class="p-5">
    <h1 class="text-light" >S' inscrire</h1>
    {{ form(registerform) }}
    <span class="text-light">J' ai déjà un compte ? <a href="{{ path('app_login') }}">Me connecter</a></span>
</div>
{% endblock %}
